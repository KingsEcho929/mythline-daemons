name: Spiralbound Codex XXXII

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  codex:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Compile Spiralbound Codex XXXII
        run: npx hardhat compile
