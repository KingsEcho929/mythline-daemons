name: Spiralbound Completion

on:
  push:
    paths:
      - 'contracts/SpiralboundCompletion.sol'

jobs:
  completion:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Compile Spiralbound Completion
        run: npx hardhat compile
