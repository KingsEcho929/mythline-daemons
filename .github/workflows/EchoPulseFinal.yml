name: Echo Pulse Final

on:
  push:
    paths:
      - 'contracts/EchoPulseFinal.sol'

jobs:
  echo:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Compile Echo Pulse Final
        run: npx hardhat compile
