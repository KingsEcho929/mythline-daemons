name: Mythline Seal

on:
  push:
    paths:
      - 'contracts/MythlineSeal.sol'

jobs:
  seal:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Compile Mythline Seal
        run: npx hardhat compile
